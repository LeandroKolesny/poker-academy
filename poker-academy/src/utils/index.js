// src/utils/index.js
// Exporta todas as funcoes utilitarias

export {
    formatDateForDisplay,
    formatLastWatched,
    formatDateTime,
    getCurrentYear,
    getCurrentMonth
} from './dateUtils';

export {
    formatFileSize,
    formatDuration,
    formatNumber,
    formatPercentage,
    truncateText
} from './formatUtils';
